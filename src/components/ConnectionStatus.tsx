import { useMotion } from '../motion/MotionProvider'
import './ouraControls.css'

const friendlyStatus: Record<string, string> = { demo: 'Demo mode', 'bridge-offline': 'Oura bridge offline', connecting: 'Connecting bridge', 'waiting-for-data': 'Waiting for ring data', calibrating: 'Calibrating', streaming: 'Live motion', reconnecting: 'Reconnecting', stopped: 'Stream stopped', disconnected: 'Disconnected', error: 'Connection needs attention' }
export function ConnectionStatus() {
  const { status, source, autoDemo, toggleAutoDemo, reset, connectOura, disconnectOura, useDemoMode, calibrate, diagnostics, updateSettings, resetMotionSettings } = useMotion(); const settings = diagnostics.settings
  return <div className="status oura-status"><span className={`status-dot ${source === 'oura' ? 'live' : ''}`} /> <span>{friendlyStatus[status]}</span>{source === 'demo' ? <><button onClick={toggleAutoDemo} aria-pressed={autoDemo}>Auto demo {autoDemo ? 'on' : 'off'}</button><button onClick={() => void connectOura()}>Connect Oura</button></> : <><button onClick={calibrate}>Calibrate</button><button onClick={() => void disconnectOura()}>Disconnect</button><button onClick={useDemoMode}>Demo mode</button></>}<button onClick={reset}>Reset</button><details className="oura-details"><summary>Live details</summary><p>{source === 'oura' ? `Samples ${diagnostics.samplesReceived} · ${diagnostics.sampleRateHz.toFixed(1)} Hz · ${diagnostics.calibrated ? 'calibrated' : 'not calibrated'}` : 'Start the local open_oura bridge, then connect. No ring key is requested by this app.'}</p>{source === 'oura' && <div className="oura-settings"><label><input type="checkbox" checked={settings.invertHorizontal} onChange={event => updateSettings({ invertHorizontal: event.target.checked })} /> Invert horizontal</label><label><input type="checkbox" checked={settings.invertVertical} onChange={event => updateSettings({ invertVertical: event.target.checked })} /> Invert vertical</label><label><input type="checkbox" checked={settings.swapAxes} onChange={event => updateSettings({ swapAxes: event.target.checked })} /> Swap axes</label><label>Dead zone <input type="number" min="0" max="15" value={settings.deadZoneDegrees} onChange={event => updateSettings({ deadZoneDegrees: Number(event.target.value) })} />°</label><label>Full scale <input type="number" min="10" max="60" value={settings.fullScaleDegrees} onChange={event => updateSettings({ fullScaleDegrees: Number(event.target.value) })} />°</label><label>Smoothing <input type="number" min=".02" max=".9" step=".02" value={settings.smoothing} onChange={event => updateSettings({ smoothing: Number(event.target.value) })} /></label><label>Counts/g <input type="number" min="100" max="3000" value={settings.countsPerG} onChange={event => updateSettings({ countsPerG: Number(event.target.value) })} /></label><button onClick={resetMotionSettings}>Reset motion settings</button></div>}</details></div>
}
