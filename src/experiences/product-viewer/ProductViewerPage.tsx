import { Link } from 'react-router-dom'
import { useState } from 'react'
import { ConnectionStatus } from '../../components/ConnectionStatus'
import { useMotion } from '../../motion/MotionProvider'
import { finishes, ProductScene, type Finish } from './ProductScene'

export function ProductViewerPage() { const [finish, setFinish] = useState<Finish>('silver'); const { reset } = useMotion(); return <main className="experience-page"><header className="experience-header"><Link to="/" className="back">← Home</Link><ConnectionStatus /></header><section className="viewer-heading"><p className="eyebrow">Experience 03 / Product viewer</p><h1>Form, in motion.</h1><p>Hand roll maps deliberately to horizontal rotation; it is not yaw tracking.</p></section><section className="viewer"><div className="scene"><ProductScene finish={finish} /></div><div className="product-copy"><span>Horizon ring</span><strong>{finishes[finish].name}</strong><p>A sculpted sleep and health companion with a seamless, sensor-forward interior.</p><ul><li>Precision titanium shell</li><li>Recessed sensing array</li><li>Comfort-first silhouette</li></ul></div><div className="finish-picker" aria-label="Select finish">{(Object.keys(finishes) as Finish[]).map(key => <button className={finish === key ? 'active' : ''} onClick={() => setFinish(key)} key={key}><i style={{ background: finishes[key].color }} />{finishes[key].name}</button>)}<button className="reset-view" onClick={reset}>Reset view</button></div></section><p className="control-hint">Move your mouse, drag the model, or use arrow keys / WASD. Space adds a motion pulse.</p></main> }
